import type { ReactNode } from 'react';

function App(): ReactNode {
  return (
    <div>
      {/* sidebar */}
      {/* content */}
      {/* placeholder for router? */}
    </div>
  );
}

export default App;
